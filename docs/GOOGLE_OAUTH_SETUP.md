# Google OAuth (removed)

The app previously supported “Sign in with Google” via `expo-auth-session`. That flow has been **removed** from MAIZE_GUARD. Authentication is **email + password** only.

If you add Google Sign-In again later, use a **development build** and follow current Expo/Google docs (or `@react-native-google-signin/google-signin`).
